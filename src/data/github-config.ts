/**
 * GitHub username used by the live GitHub stats widget (GithubStats /
 * lib/github.ts).
 *
 * TODO: replace "REPLACE_ME" with the real GitHub username. Until then the
 * widget renders nothing (getGithubStats returns null without fetching), so
 * the placeholder is safe to ship but shows no data.
 */
export const GITHUB_USERNAME = "REPLACE_ME";
